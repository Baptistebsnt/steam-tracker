meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "test@test.com",
    "password": "password123"
  }
}

script:post-response {
  bru.setEnvVar("token", res.body.token);
}